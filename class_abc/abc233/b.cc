#include <iostream>
#include <vector>
#include <algorithm>

using ll = long long int;

int main() {

	std::ios_base::sync_with_stdio(false);
	std::cin.tie(NULL);

    ll n; 
	std::cin >> n;

    std::vector<int> vec(n, 0);
    for (int i = 0; i < n; ++i) {
    }

}